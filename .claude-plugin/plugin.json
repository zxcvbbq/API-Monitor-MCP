{
  "name": "api-monitor-mcp",
  "version": "0.10.225",
  "description": "MCP bridge for Rohitab API Monitor captures and GUI.",
  "mcpServers": {
    "api-monitor": {
      "command": "uv",
      "args": ["run", "--project", "${CLAUDE_PLUGIN_ROOT}", "python", "server.py"]
    }
  }
}
