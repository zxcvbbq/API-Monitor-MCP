[project]
name = "rohitab-api-monitor-mcp"
version = "0.10.180"
description = "MCP bridge for Rohitab API Monitor captures and GUI"
requires-python = ">=3.10"
dependencies = ["mcp>=1.0.0,<2", "pywinauto>=0.6.8"]

[tool.ruff]
line-length = 100
